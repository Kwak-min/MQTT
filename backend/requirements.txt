# backend/requirements.txt
# ─────────────────────────────────────────────────────────────────────────────
# Production dependencies for the IoT Gateway Backend.
# Install with:  pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# REST API framework
flask>=3.0.0

# CORS headers for React/Vue dev server (localhost:3000)
flask-cors>=4.0.0

# Environment variable loading (.env file support — optional but ready)
python-dotenv>=1.0.0

# MQTT 클라이언트 — ESP32 설정 동기화 (gingerbread/config 토픽 QoS 1 발행)
# ConfigService가 config.json 변경 시 브로커를 통해 ESP32에 즉시 푸시합니다.
paho-mqtt>=1.6.1
