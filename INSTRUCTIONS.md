# [Specification] IoT Battery-Saving Architecture with Wireless Power Interception

본 문서는 초경량, 저전력 IoT 자율 자산 감시 시스템의 물리 하드웨어 결선, 펌웨어 제어 시나리오 및 네트워크 데이터 흐름을 정의한 마스터 개발 명세서입니다. 모든 서브시스템 개발자(Firmware, Backend, Full-Stack)는 본 명세서에 정의된 하드웨어 핀 매핑과 시퀀스 로직을 엄격히 준수하여 구현해야 합니다.

---

## 1. 시스템 개요 및 핵심 메커니즘
본 시스템은 CR2032 등 코인 셀 배터리로 동작하는 초소형 단말의 생존 주기를 극대화하기 위해 **"유리 벌브(Glass Bulb)형 스프링클러"** 제어 아키텍처를 임베디드 소프트웨어로 재해석하여 구현합니다.

* **상시 차단 (Deep Sleep)**: 평소 메인 통신 단말은 무선 모듈을 끄고 MCU 코어를 Deep Sleep 상태로 유지하여 전력 소모를 최소화합니다.
* **센서 트리거 (Wake-up)**: 상시 가동되는 초저전력 환경 센서가 위험 임계치를 감지하는 순간 하드웨어 인터럽트를 발생시켜 메인 단말을 깨웁니다.
* **버스트 전송 (Burst Transmission)**: 깨어난 단말은 최소한의 시간 동안 무선 패킷을 집중적으로 방출(Burst)한 후 다시 즉시 Deep Sleep으로 진입합니다.
* **독립식 전력 계측**: 메인 단말의 전력 소비 변동 추이(Deep Sleep vs Active 버스트 전송)를 계측 오차 없이 감시하기 위해, 전원 공급 길목에 전류 센서를 직렬 삽입(Intercept)하고 이를 전담 MCU가 무선으로 취합하는 분리형 계측 구조를 채택합니다.

---

## 2. 하드웨어 물리 배선 명세 (Hardware Pin Mapping)

빵판(Breadboard) 내부의 구리 철사 저항으로 인한 전압 강하 및 계측 오차를 방지하기 위해, **INA226 전력 측정 센서 모듈 2개는 빵판에 꽂지 않고 공중에 선으로만 띄워서 전원 길목에 직렬 연결(인터셉트)**합니다.

### 2.1 메인 구동 및 전력 가로채기(Intercept) 회로
시스템은 완전히 동일한 전원 구조를 가진 2개의 독립된 노드(Node A, Node B)로 구성됩니다.

#### [Node A / Node B 공통 전원단 결선]
1. **소켓 전원 입력**: 보조배터리에서 나온 USB-C 케이블을 [USB C타입 4핀 암 소켓 변환보드]에 연결합니다.
2. **소켓 핀 정의**: 데이터 선(D+, D-)은 전면 비워두고, 전원 핀(V, G)만 사용합니다.
3. **플러스(+)선 인터셉트**: C타입 소켓의 V 구멍 ──> INA226 센서 모듈의 굵은 고전류 터미널 **IN+** 단자에 직결 및 나사 고정.
4. **마이너스(-)선 직결**: C타입 소켓의 G 구멍 ──> 각 메인 노드(YD-ESP32-S3)의 **GND** 핀에 직결.
5. **센서 출력단**: INA226 센서 모듈의 굵은 고전류 터미널 **IN-** 단자 ──> 각 메인 노드(YD-ESP32-S3)의 **5V (VIN)** 핀에 연결.

#### ⚠️ 필수 하드웨어 튜닝 및 가공 (납땜 가이드)
* **자체 전압 측정(VBUS) 점프**: INA226 센서가 인가되는 전압을 인지할 수 있도록, 센서의 굵은 구멍 **IN+ 단자에서 전선을 한 가닥 분기(점프)하여 센서 일렬 핀 단자의 VBS 핀에 납땜**하여 쇼트시켜야 합니다.
* **I2C 주소 분리 (하드웨어 어드레싱)**: 데이터 취합 시 버스 충돌을 막기 위해 두 센서의 주소를 이원화합니다.
  * **Node A 전력 센서**: 순정 상태 유지 (기본 I2C Address: 0x40)
  * **Node B 전력 센서**: 센서 기판 표면의 **A0 구리 패드와 VCC 구리 패드를 납량으로 뭉쳐서 쇼트**시킵니다. (변경 후 I2C Address: 0x41)

### 2.2 전력 데이터 수집 회로 (Sub-System)
두 센서(0x40, 0x41)의 미세 전류 데이터는 메인 노드를 거치지 않고, 별도의 전력 계측 전담 MCU로 취합됩니다.

* **INA226 센서 A (0x40) & B (0x41)의 일렬 핀 단자** ──> **전력 측정 전용 MCU (ESP32-C3 Mini)**
  * 양쪽 센서의 VCC ──> ESP32-C3의 3.3V 핀 (병렬 연결)
  * 양쪽 센서의 GND ──> ESP32-C3의 GND 핀 (병렬 연결)
  * 양쪽 센서의 SDA ──> ESP32-C3의 하드웨어 SDA 지정 핀 (병렬 연결, 기본 GPIO 8)
  * 양쪽 센서의 SCL ──> ESP32-C3의 하드웨어 SCL 지정 핀 (병렬 연결, 기본 GPIO 9)

---

## 3. 네트워크 및 무선 통신 프로토콜 명세 (Wireless Network)

시연장 환경의 2.4GHz 주파수 포화 상태를 대비하여 모든 보드와 서버는 고정된 사설 무선 서브넷 내부에서만 통신합니다.

* **네트워크 환경 인프라**: 독립된 외부 무선 AP(공유기) 또는 팀원 스마트폰의 2.4GHz 테더링 활성화.
* **네트워크 식별자**: SSID = Capstone_QoS_Test / 패스워드 = 12345678 (Firmware 내 하드코딩)
* **중앙 서버 인프라**: 라즈베리파이 5 (Static IP 지정 운영 권장, 예: 192.168.1.100)

### 3.1 노드별 통신 대상 및 대상 포트 매핑

* **Node A (ESP32-S3)** ──> Wi-Fi (TCP) ──> MQTT 패킷 전송 ──> **[라즈베리파이 5] Port 1883**
* **Node B (ESP32-S3)** ──> Wi-Fi (UDP) ──> Gingerbread 패킷 전송 ──> **[라즈베리파이 5] Port 5000**
* **전력 MCU (ESP32-C3)** ──> Wi-Fi (UDP/HTTP) ──> JSON 전력 데이터 스트리밍 ──> **[라즈베리파이 5] Port 6000**

1. **Node A (Standard MQTT Node)**: Wi-Fi 접속 후 MQTT 브로커(Mosquitto)의 지정 토픽(room/env/nodeA)으로 환경 센서 JSON 데이터를 TCP 기반 전송.
2. **Node B (Low-Power Gingerbread UDP Node)**: 세션 연결 오버헤드를 최소화하기 위해 와이파이 연결 즉시 지정된 UDP 포트(5000)로 초경량 바이너리/스트링 패킷을 게이트웨이로 비연결형(Connectionless) 전송.
3. **전력 전담 MCU (ESP32-C3)**: 상시 구동 상태에서 두 센서의 전류(mA), 전압(V), 전력(mW) 데이터를 바인딩하여 100ms 주기로 게이트웨이 서버의 수신 포트(6000)로 전송.

---

## 4. 소프트웨어 및 펌웨어 시퀀스 로직 (Sequence Logic)

### 4.1 메인 노드(ESP32-S3) 펌웨어 제어 시나리오
1. **Booting**: 전원이 인가되면 하드웨어 초기화 후 즉시 외부 BME 환경 센서의 상태를 확인합니다.
2. **Local Machine Learning (TinyML Inference)**: BME 센서로부터 수집된 가스, 습도, 온도 데이터를 로컬에 빌드된 초소형 신경망 모델에 입력하여 현재 환경의 위험 등급(QoS Level)을 추론합니다.
3. **Condition Branch**:
   * **정상 상태 (Normal)**: 통신 모듈(Wi-Fi)을 깨우지 않고, 외부 인터럽트 핀을 활성화한 채 즉시 하드웨어 딥슬립 모드(esp_deep_sleep_start())로 진입합니다. (소모 전류 억제)
   * **위험 상태 (Event Triggered)**: 즉시 내부 Wi-Fi 스택을 인에이블하고 라즈베리파이 서버와 동기화하여 지정된 프로토콜(Node A: MQTT, Node B: UDP)로 경고 패킷 및 센서 데이터를 방출합니다.
4. **Return to Sleep**: 패킷 전송 완료 플래그가 확인되면 지연 시간 없이 다시 딥슬립 모드로 전환합니다.

### 4.2 전력 계측 MCU(ESP32-C3) 펌웨어 제어 시나리오
1. **Initialize**: I2C 버스를 활성화하고 0x40 센서와 0x41 센서의 설정 레지스터를 호출하여 컨버전 타임 및 에버리징 모드를 최적화합니다.
2. **Continuous Monitoring**: 100ms 타임 루프 내에서 두 센서의 션트 저항 전압 강하 값을 읽어 순시 전류 및 전력 소비량을 연산합니다. 메인 보드가 딥슬립 상태일 때의 미세 전류와 Active 상태일 때의 버스트 전류를 무손실로 추적해야 합니다.
3. **Data Stream**: 수집된 두 채널의 시계열 전력 데이터를 패킷화하여 라즈베리파이 게이트웨이 서버로 실시간 무선 스트리밍합니다.

### 4.3 게이트웨이 및 대시보드(라즈베리파이 5 서버) 시스템 명세
* **MQTT Broker**: TCP 1883 포트를 개방하여 Node A의 MQTT 토픽 구독(Subscribe).
* **UDP Socket Server**: 비동기 소켓 리스너를 구동하여 Node B의 Gingerbread 패킷(Port 5000) 및 ESP32-C3의 전력 스트리밍 데이터(Port 6000) 가로채기(Listen).
* **Full-Stack 시각화**: 수신된 데이터를 파싱하여 오프라인 데이터베이스(InfluxDB 또는 SQLite)에 적재하고, 프론트엔드 대시보드 화면에 **"Node A/B의 통신 방식 및 딥슬립 주기 변화에 따른 물리 전력 소모(mA) 그래프"**를 실시간 시계열로 매핑하여 모니터링 창에 출력합니다.