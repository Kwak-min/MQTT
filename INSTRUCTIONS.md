# 프로젝트명: Gingerbread (지능형 저전력 MQTT-SN 프로토콜)

## 1. 프로젝트 개요
본 프로젝트는 UDP 기반의 MQTT-SN 프로토콜을 커스텀 설계하고 온디바이스 AI(TinyML)를 결합하여, 실시간 복합 환경 데이터의 위험도와 무선 네트워크 가용성을 다차원적으로 분석해 QoS 레벨(0, 1, 2)을 동적으로 결정하는 지능형 저전력 통신 시스템을 구축하는 것을 목표로 함.

## 2. 시스템 아키텍처
- **Sensor Client**: YD-ESP32-S3 기반 엣지 단말 (BME680 복합 환경 및 가스 데이터 수집, TinyML 복합 추론 엔진 구동, 가변 QoS 패키징 및 초저전력 Deep Sleep 제어)
- **Gateway Server**: 라즈베리파이 5 기반 모듈형 Python UDP 백엔드 서버 (QoS 0/1/2 단계별 핸드셰이크 중계, 데이터 파싱, 클라이언트 세션 관리 및 향후 웹 프론트엔드 연동을 위한 REST API 컨트롤러 탑재)
- **Log/Analysis**: CSV 기반 하드웨어 전력 수모량 및 통신 로그 실시간 저장 및 관리 파이프라인 구축

## 3. 프로토콜 상세 규격 (QoS 2 확장 반영)
### 필수 제어 메시지 타입
- **CONNECT**: 클라이언트 접속 및 세션 생성 (수면 주기 동기화)
- **PUBLISH**: 센서 데이터 및 AI 에이전트 추론 메타데이터 전송 (QoS 0, 1, 2 적용)
- **PUBACK**: QoS 1 PUBLISH 메시지에 대한 수신 확인 응답
- **PUBREC**: QoS 2 단계 1 - 서버측 수신 확인 통지 패킷
- **PUBREL**: QoS 2 단계 2 - 클라이언트측 세션 방출 통지 패킷
- **PUBCOMP**: QoS 2 단계 3 - 서버측 절차 최종 완료 통지 패킷
- **DISCONNECT**: 하드웨어 Deep Sleep 진입 전 세션 수면(Asleep) 상태 동기화 알림

### QoS 가동 및 단계별 재전송 제어 규격
- **Timeout**: 각 핸드셰이크 단계별 대기 제한 시간 2.0초 설정
- **Max Retry**: 각 제어 단계별 최대 3회 재전송 시도 제한 (초과 시 유실 예외 처리)
- **메시지 식별**: `msg_id` 기반 패킷 추적을 통한 무선 구간 데이터의 누락 및 중복 수신 방지

### 세션 관리 테이블 필드 (Gateway 운영 기준)
- `client_id`, `status` (active/asleep), `topic_id`, `last_seen`, `pending_msg`

## 4. 지능형 QoS 3단계 동적 자동 선택 로직
AI 에이전트(TinyML)가 수집된 지표를 바탕으로 다차원 의사결정을 수행하여 최적의 QoS 레벨을 바인딩함.

1. **입력 파라미터 정의**
   - **Data Urgency (데이터 긴급도)**: BME680 계측 데이터 기반 위험 수치 판정 (0: 정상, 1: 긴급 화재 및 가스 누출 위험 상황)
     - *판정 기준*: 온도가 30도 초과 혹은 10도 미만으로 가거나, 실내 공기질 위험을 대변하는 가스 저항(Gas Resistance) 수치가 임계치 미만으로 급격히 낙하할 때 긴급 상황으로 진단.
   - **Network Status (네트워크 상태)**: RSSI, RTT, 유실률 기반 무선 선로 판정 (0: 양호, 1: 불안정/장애 선로)

2. **3단계 제어 조건 매트릭스**
   - **QoS 0 (최대 저전력 모드)**: Data Urgency == 0 **AND** Network Status == 0 (무확인 단발성 전송 후 즉시 Sleep)
   - **QoS 1 (신뢰성 보장 모드)**: 두 지표 중 하나만 불안정 상황에 해당할 때 (2단계 핸드셰이크 제어)
   - **QoS 2 (최고 신뢰성 모드)**: Data Urgency == 1 **AND** Network Status == 1 (가스/온도 위험 상황 및 극심한 통신 장애가 동시에 발생했을 때, 데이터 무결성 절대 보장을 위한 4단계 핸드셰이크 강제 가동)

## 5. 패킷 최적화 규격
- 데이터 전송 효율 향상 및 무선 통신 구간 오버헤드 극소화를 위해 정적 구조체 정렬(#pragma pack(push, 1)) 정책을 준수함.
- 패킷 헤더 내에 1바이트 공간을 할당하고 **비트필드(Bit-field)** 구조를 도입하여 `network_status`(2비트) 및 `data_urgency`(2비트) 형태의 AI 추론 결과 메타데이터를 초경량 패킹 처리함.

## 6. 실험 및 평가 지표
### 네트워크 실험 (tc netem 활용 환경 모사)
- **테스트 시나리오**: 정상, 패킷 Loss 5%, 패킷 Loss 10%, 선로 Delay 100ms, 지연+손실 복합 장애 환경
- **비교 지표**: 표준 고정형 통신 방식 대비 복합 제어 Gingerbread 에이전트 방식의 배터리 사용 수명(전력 차단 면적 분석) 및 패킷 수신 성공률(PDR) 정량 비교 분석

### 핵심 데이터 로그 필드
- `time`, `scenario`, `qos`, `msg_id`, `success`, `retry_count`, `temp`, `hum`, `gas`, `power_consumption(mA)`